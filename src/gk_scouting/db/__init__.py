"""
gk_scouting.db
--------------

Camada de persistência (PostgreSQL via SQLAlchemy). Fase 1 do roadmap de
arquitetura: apenas a tabela `gk_performances`, sem repositório nem API
ainda -- ver `models.py` para o schema e `config.py` para a ligação.
"""
