[pytest]
# Torna `src/` importável nos testes sem exigir instalação do pacote.
# Também corrige o problema de `pytest` (sem `python -m`) falhar a importar
# os módulos do projeto.
pythonpath = src
testpaths = tests
